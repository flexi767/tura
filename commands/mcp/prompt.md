Use `mcp` only for servers explicitly configured by the user. Call `list_tools` before using an unfamiliar server. Invoke only tools allowed by the server configuration. Configuration is read from `$TURA_HOME/mcp.json`, or `<workspace>/.tura/mcp.json` when no explicit home is set.

